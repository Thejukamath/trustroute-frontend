# Copy to .env (or .env.production) and point at your deployed backend.
# Leave unset to use the local Vite dev proxy (http://localhost:4000 for /api).
VITE_API_URL=https://trustroute-backend.onrender.com